#include <bits/stdc++.h>
using namespace std;
const int maxn = 300000 + 20;
int a[maxn];
int main() {
  ios::sync_with_stdio(false);
  cin.tie(0);

  int n, m;
  cin >> n >> m;
  for (int i = 0; i < n; ++i) {
    cin >> a[i];
  }
  int x;
  for (int i = 0; i < m; ++i) {
    cin >> x;
  }
  return 0;
}
