#include <iostream>
using namespace std;

class TaskF {
public:
	void solve(std::istream& in, std::ostream& out) {
		int n;
		in >> n;

		for (int i = 0; i < n; ++i) {
			int p;
			char c;
			in >> p >> c;
		}

		out << ans;
	}
};
