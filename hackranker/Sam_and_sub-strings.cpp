#include <bits/stdc++.h>
using namespace std;
using ll = long long;
int main() {
  string str;
  while (cin >> str) {
    int slen = str.size();
    ll ans = 0;
    ll temp = 0;
    for (int i = 0; i < slen; ++i) {
      temp = temp * 10 + (i + 1) * 
    }
  }
  return 0;
}
