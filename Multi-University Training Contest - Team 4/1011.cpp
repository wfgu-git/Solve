#include <bits/stdc++.h>
using namespace std;
char grid[7][21];
int num[]
int main() {
  int T;
  scanf("%d", &T);
  for (int cas = 1; cas <= T; ++cas) {
    for (int i = 0; i < 7; ++i) {
      scanf("%s", grid);
    }
    int h1, h2, m1, m2;

    printf("%d%d:%d%d\n", h1, h2, m1, m2);
  }
  return 0;
}
